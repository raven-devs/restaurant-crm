{
  "framework": "vite",
  "buildCommand": "npm install && npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://restaurant-crm-production-4ed3.up.railway.app/api/:path*"
    }
  ]
}
